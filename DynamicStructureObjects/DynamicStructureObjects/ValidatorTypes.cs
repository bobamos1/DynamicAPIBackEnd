namespace DynamicStructureObjects
{
    public enum ValidatorTypes
    {
        [Value("Required")]
        REQUIRED = 1,
        [Value("Max")]
        MAX = 2,
        [Value("Min")]
        MIN = 3
    }
}
