namespace DynamicStructureObjects
{
    public enum BaseRoutes
    {
        [Value("NULL")]
        NONE = 1,
        [Value("GetAll")]
        GETALL = 2,
        [Value("Get")]
        GET = 3,
        [Value("INSERT")]
        INSERT = 4,
        [Value("UPDATE")]
        UPDATE = 5,
        [Value("GetAllDetailed")]
        GETALLDETAILED = 6,
        [Value("GetDetailed")]
        GETDETAILED = 7,
        [Value("CBO")]
        CBO = 8,
    }
}
